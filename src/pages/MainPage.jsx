export function MainPage() {
  return (
    <div
      style={{
        color: 'var(--card-blue)',
        backgroundColor: 'var(--sticker-blue-1)',
        textAlign: 'center',
        padding: '1rem 0',
        fontSize: '1.6rem',
      }}
    >
      MainPage
    </div>
  );
}
