export function Header() {
  return (
    <div
      style={{
        backgroundColor: 'var(--background-primary)',
        textAlign: 'center',
        padding: '1rem 0',
        fontSize: '1.6rem',
      }}
    >
      Header
    </div>
  );
}
